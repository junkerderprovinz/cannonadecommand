Menu="Buttons:97"
Title="CannonadeCommand"
Icon="cannonadecommand.png"
Link="cc-shares-hook"
---
<?php
/* Registered under Menu="Buttons" so Unraid loads this page's assets into the <head> of every
   page: sheets/CannonadeCommand.Shares.css (auto-injected because the basename matches this
   .page) restyles the tabbed sub-page nav on /Shares and /Docker, and the script below loads the
   shares enhancer. Link="cc-shares-hook" renders a bare <div> instead of a visible icon. All
   styling is gated behind html.cc-shares-on, which the enhancer sets only when the "Freigaben"
   area is enabled in CC settings. */
?>
<script defer src="<?autov('/plugins/cannonadecommand/scripts/shares.js')?>"></script>
