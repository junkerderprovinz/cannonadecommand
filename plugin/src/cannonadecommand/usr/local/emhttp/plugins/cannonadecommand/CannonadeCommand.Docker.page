Menu="Docker"
Icon="rocket"
Tag="rocket"
---
<?php
/* Injected into Unraid's Docker tab. The page has no Title and no content of its own, which
   would render as a stray bar; it only loads the styles and the script, which enhance the
   existing container list in place. Data comes from the same-origin PHP proxy. */
?>
<link type="text/css" rel="stylesheet" href="<?autov('/plugins/cannonadecommand/styles/docker.css')?>">
<script defer src="<?autov('/plugins/cannonadecommand/scripts/docker.js')?>"></script>
