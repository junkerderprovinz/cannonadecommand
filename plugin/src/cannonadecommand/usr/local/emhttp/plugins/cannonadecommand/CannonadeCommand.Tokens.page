Menu="Buttons:94"
Title="CannonadeCommand"
Icon="cannonadecommand.png"
Link="cc-tokens-hook"
---
<?php
/* Menu="Buttons" makes Unraid auto-inject sheets/CannonadeCommand.Tokens.css into the <head> of
   every page. It defines the semantic colour tokens and the size canon once on <html>, so every
   CC sheet inherits them and follows all Unraid themes (the light override keys on
   Theme--white/azure) and the accent/rainbow modes. Loaded at :94, before the area sheets at
   :95+, so the tokens exist first. Link="cc-tokens-hook" renders a bare <div>, no icon.

   cc-theme.js loads synchronously rather than deferred, so window.CCTheme (idealText, the
   persisted rainbow seed, palette/rbColor) exists before the deferred area scripts run. */
?>
<script src="<?autov('/plugins/cannonadecommand/scripts/cc-theme.js')?>"></script>
