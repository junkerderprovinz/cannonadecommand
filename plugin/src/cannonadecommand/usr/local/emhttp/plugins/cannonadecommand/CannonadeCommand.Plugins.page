Menu="Plugins"
Icon="cannonadecommand.png"
---
<?php
/* CannonadeCommand - injected into Unraid's native PLUGINS tab. Renders NO
   visible panel of its own; it only loads the styles + the script, which
   decorate the existing #plugin_table in place (badges, pills, accent/rainbow),
   exactly like the Docker-tab enhancer. */
?>
<link type="text/css" rel="stylesheet" href="<?autov('/plugins/cannonadecommand/styles/docker.css')?>">
<script defer src="<?autov('/plugins/cannonadecommand/scripts/plugins.js')?>"></script>
