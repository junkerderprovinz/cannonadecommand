Menu="Buttons:98"
Title="CannonadeCommand"
Icon="cannonadecommand.png"
Link="cc-vmtab-hook"
---
<?php
/* Registered under Menu="Buttons" so Unraid auto-injects sheets/CannonadeCommand.VmTab.css into
   the <head> of every page, like the Header/Shares/SettingsGrid hooks; a <link> in a Menu="VMs"
   page does not load. All rules are gated behind html.cc-vms-on, which vms.js sets only when the
   VM area and master theming are on. Link="cc-vmtab-hook" renders a bare <div> instead of a
   visible icon.

   vms.js loads here as well: /VMs is Tabs="true", so a title-less child page goes through the
   tabbed layout's inline-eval branch, which does not execute <script> tags. vms.js gates itself
   to /VMs. */
?>
<script defer src="<?autov('/plugins/cannonadecommand/scripts/vms.js')?>"></script>
